/*const fs = require('fs');
function copyFile(fileName) {
    const contentFile = fs.readFileSync('./in/' + fileName);
    fs.writeFileSync('./out/' + fileName, contentFile);
}
module.exports = copyFile;
*/